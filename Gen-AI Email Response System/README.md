# Gen-AI Email Response System

A small Python project that generates and evaluates automated email responses using a dataset of emails.

## Project Structure

- `app.py` — Streamlit web app (UI) to interact with the generator.
- `main.py` — Optional entry point used for running scripts.
- `src/generator.py` — Generates reply suggestions from input emails.
- `src/evaluvator.py` — Evaluates generated replies against ground truth (note: evaluator name is `evaluvator.py`).
- `data/emails_dataset.csv` — Dataset of example emails for training/evaluation.
- `data/evaluation_results.csv` — Output file collecting evaluation results.
- `requirements.txt` — Python dependencies.
- `pyproject.toml` — Project metadata and build config (if used).

## Quick Start

1. Create and activate a virtual environment (recommended):

```bash
python -m venv .venv
# Windows
.\.venv\Scripts\activate
# macOS / Linux
source .venv/bin/activate
```

2. Install dependencies:

```bash
pip install -r requirements.txt
```

3. Run the Streamlit app:

```bash
streamlit run app.py
```

Or run a script with `python main.py` if provided.

## Usage

- Open the Streamlit UI at the address shown in the terminal (usually http://localhost:8501).
- Use the interface to load example emails and generate replies.
- Evaluation outputs are saved to `data/evaluation_results.csv`.

## Notes & Tips

- The evaluator filename `evaluvator.py` appears to contain a typo; consider renaming to `evaluator.py` if you want consistency.
- If you add API keys or secrets, keep them out of source control and store them in a `.env` file.

## Contributing

Contributions are welcome. Open an issue or submit a pull request with a clear description and tests where appropriate.

## License

Specify a license for the project (e.g., MIT). If you want, tell me which license to add and I'll insert the badge and text.

---

Generated by GitHub Copilot assistant.
